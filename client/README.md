# React Native Boilerplate

An Expo + TypeScript starter with:

- Welcome screen
- Auth screen
- Three-tab home navigation

## Run

1. Install dependencies: `npm install`
2. Start the app: `npm run start`
3. Open on iOS, Android, or web from the Expo dev tools